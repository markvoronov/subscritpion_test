env: "local" #local, dev, prod
http_server:
  address: "0.0.0.0"
  port: "8080"
  timeout: "4s"
  idle_timeout: "60s"
  shutdown_timeout: "10s"

database:
  driver: "postgres"
  host: "db"
  port: "5432"
  user: "postgres"
  password: "postgres"
  name: "subscriptions"
  sslmode: "disable"
  pool:
    max_open_conns: 10
    max_idle_conns: 5
    conn_lifetime: "5m"
